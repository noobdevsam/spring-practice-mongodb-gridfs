spring:
    application:
        name: spring-practice-mongodb-gridfs
    threads:
        virtual:
            enabled: true
    servlet:
        multipart:
            max-file-size: 100MB
            max-request-size: 100MB

    data:
        mongodb:
            uri: mongodb://myuser:mypassword@localhost:27017/mydatabase?authSource=mydatabase

logging:
    level:
        org.springframework.data.mongodb: DEBUG